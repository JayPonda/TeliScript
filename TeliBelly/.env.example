# The target for the Vite proxy. 
# For local development, this is typically http://localhost:8000
# For Docker Compose, this would be the name of the backend service.
VITE_API_TARGET=http://backend:8000
